Convert the input description into a hierarchical JSON structure that represents the logic of the description. 
The argument-count should match the number of arguments. Mark the node that should be used to answer the question.
Correct spelling mistakes. Only use the recent world-context, not the context from the example (e.g., don't refer to port-A or Bluesky).
node types: attribute-of, apply-property, haversine-formula, when, count, convert-units, while, max, apply-relation, difference, multiply, add, divide, subtract, and, equal, less-than, greater-than
